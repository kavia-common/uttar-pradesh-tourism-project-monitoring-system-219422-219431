# Local profile for developers. Prefer setting environment variables instead of editing this file.
# Activate with: SPRING_PROFILES_ACTIVE=local

server.port=3001
spring.application.name=project-monitoring-backend

# Recommend setting these via environment variables (DB_*). These are commented out intentionally.
# spring.datasource.url=jdbc:postgresql://localhost:5432/uptourism_pms?sslmode=disable
# spring.datasource.username=postgres
# spring.datasource.password=postgres

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
